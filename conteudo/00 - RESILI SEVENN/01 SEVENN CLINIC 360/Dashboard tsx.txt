import { useState, useEffect } from "react";
import {
    Users,
    Calendar,
    TrendingUp,
    DollarSign,
    ArrowUpRight,
    ArrowDownRight,
    Activity,
    ChevronRight,
    Search,
    Filter,
    CalendarDays,
    Briefcase,
    Zap
} from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import {
    AreaChart,
    Area,
    XAxis,
    YAxis,
    CartesianGrid,
    Tooltip,
    ResponsiveContainer,
    FunnelChart,
    Funnel,
    LabelList,
    Cell
} from 'recharts';
import { CylindricalFunnel } from "@/components/dashboard/CylindricalFunnel";
import { useAuth } from "@/contexts/AuthContext";
import {
    Select,
    SelectContent,
    SelectItem,
    SelectTrigger,
    SelectValue,
} from "@/components/ui/select";
import { cn } from "@/lib/utils";

// --- DUMMY DATA FOR VISUALIZATION ---

const dataSales = [
    { name: '01', revenue: 4500, profit: 1200 },
    { name: '05', revenue: 5200, profit: 1500 },
    { name: '10', revenue: 4800, profit: 1300 },
    { name: '15', revenue: 6100, profit: 1900 },
    { name: '20', revenue: 5500, profit: 1700 },
    { name: '25', revenue: 7200, profit: 2400 },
    { name: '30', revenue: 6800, profit: 2200 },
];

const dataFunnel = [
    { value: 100, name: 'Leads', fill: '#94a3b8' },
    { value: 65, name: 'Agendados', fill: '#3b82f6' },
    { value: 45, name: 'Compareceram', fill: '#8b5cf6' },
    { value: 28, name: 'Venderam', fill: '#59A52C' },
];

const todayAppointments = [
    { id: 1, patient: "Mariana Costa", time: "09:00", service: "Botox - Testa", status: "confirmed", professional: "Dra. Ana Silva" },
    { id: 2, patient: "Ricardo Alvez", time: "10:30", service: "Avaliação", status: "arrived", professional: "Dr. Marcos Farias" },
    { id: 3, patient: "Juliana Mendes", time: "11:00", service: "Preenchimento Labial", status: "confirmed", professional: "Dra. Ana Silva" },
    { id: 4, patient: "Carla Oliveira", time: "14:00", service: "Bioestimulador", status: "pending", professional: "Dra. Beatriz Santos" },
    { id: 5, patient: "Bruno Santos", time: "15:30", service: "Retorno", status: "pending", professional: "Dr. Marcos Farias" },
];

const crmRecent = [
    { id: 1, action: "Lead Qualificado", name: "Helena Rocha", value: 3500, time: "2h atrás", type: "lead" },
    { id: 2, action: "Agendamento Realizado", name: "Fábio Lima", value: 1200, time: "4h atrás", type: "schedule" },
    { id: 3, action: "Venda Concluída", name: "Sandra Peron", value: 5800, time: "5h atrás", type: "sale" },
    { id: 4, action: "Novo Lead", name: "Carlos Magno", value: 0, time: "6h atrás", type: "lead" },
    { id: 5, action: "Orçamento Enviado", name: "Roberto Dias", value: 15600, time: "7h atrás", type: "lead" },
];

// --- END DUMMY DATA ---

export default function Dashboard() {
    const { profile, clinic } = useAuth();
    const [greeting, setGreeting] = useState("");
    const [dateRange, setDateRange] = useState("this-month");

    useEffect(() => {
        const hour = new Date().getHours();
        if (hour < 12) setGreeting("Bom dia");
        else if (hour < 18) setGreeting("Boa tarde");
        else setGreeting("Boa noite");
    }, []);

    const formatCurrency = (value: number) => {
        return new Intl.NumberFormat("pt-BR", {
            style: "currency",
            currency: "BRL",
            maximumFractionDigits: 0
        }).format(value);
    };

    return (
        <div className="space-y-6 pb-10">
            {/* Header / Welcome */}
            <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
                <div>
                    <h1 className="text-3xl font-bold tracking-tight bg-gradient-to-r from-foreground to-foreground/70 bg-clip-text">
                        {greeting}, {profile?.full_name?.split(' ')[0] || 'Doutor(a)'} 👋
                    </h1>
                    <p className="text-muted-foreground mt-1">
                        Aqui está o que está acontecendo na <span className="text-primary font-semibold">{clinic?.name}</span> hoje.
                    </p>
                </div>
                <div className="flex items-center gap-3">
                    <div className="flex items-center gap-3">
                        <span className="text-sm font-medium text-muted-foreground hidden md:inline-block">Período:</span>
                        <Select value={dateRange} onValueChange={setDateRange}>
                            <SelectTrigger className="w-[180px] h-10 bg-background">
                                <CalendarDays className="w-4 h-4 mr-2 text-muted-foreground" />
                                <SelectValue placeholder="Selecione o período" />
                            </SelectTrigger>
                            <SelectContent>
                                <SelectItem value="today">Hoje</SelectItem>
                                <SelectItem value="this-week">Esta Semana</SelectItem>
                                <SelectItem value="this-month">Este Mês</SelectItem>
                                <SelectItem value="custom">Personalizado</SelectItem>
                            </SelectContent>
                        </Select>
                    </div>
                </div>
            </div>

            {/* KPI Section - 5 Columns Grid */}
            <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-5 gap-4">

                {/* 1. RECEITA (Faturamento) */}
                <Card className="border-l-4 border-l-emerald-500 shadow-sm hover:shadow-md transition-all">
                    <CardHeader className="flex flex-row items-center justify-between pb-2 space-y-0">
                        <CardTitle className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Receita</CardTitle>
                        <div className="p-1.5 bg-emerald-50 dark:bg-emerald-500/10 rounded-full">
                            <DollarSign className="w-3.5 h-3.5 text-emerald-600" />
                        </div>
                    </CardHeader>
                    <CardContent>
                        <div className="text-2xl font-bold text-slate-900 dark:text-slate-50">{formatCurrency(120500)}</div>
                        <div className="flex items-center gap-1.5 mt-1 text-emerald-600 text-xs font-semibold">
                            <ArrowUpRight className="w-3 h-3" />
                            <span>14.5%</span>
                        </div>
                    </CardContent>
                </Card>

                {/* 2. VENDAS (Fechamentos) */}
                <Card className="border-l-4 border-l-blue-500 shadow-sm hover:shadow-md transition-all">
                    <CardHeader className="flex flex-row items-center justify-between pb-2 space-y-0">
                        <CardTitle className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Vendas</CardTitle>
                        <div className="p-1.5 bg-blue-50 dark:bg-blue-500/10 rounded-full">
                            <Users className="w-3.5 h-3.5 text-blue-600" />
                        </div>
                    </CardHeader>
                    <CardContent>
                        <div className="text-2xl font-bold text-slate-900 dark:text-slate-50">42</div>
                        <div className="flex items-center gap-1.5 mt-1 text-blue-600 text-xs font-semibold">
                            <ArrowUpRight className="w-3 h-3" />
                            <span>8 novas</span>
                        </div>
                    </CardContent>
                </Card>

                {/* 3. TICKET MÉDIO */}
                <Card className="border-l-4 border-l-amber-500 shadow-sm hover:shadow-md transition-all">
                    <CardHeader className="flex flex-row items-center justify-between pb-2 space-y-0">
                        <CardTitle className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Ticket Médio</CardTitle>
                        <div className="p-1.5 bg-amber-50 dark:bg-amber-500/10 rounded-full">
                            <TrendingUp className="w-3.5 h-3.5 text-amber-600" />
                        </div>
                    </CardHeader>
                    <CardContent>
                        <div className="text-2xl font-bold text-slate-900 dark:text-slate-50">{formatCurrency(2869)}</div>
                        <div className="flex items-center gap-1.5 mt-1 text-red-500 text-xs font-semibold">
                            <ArrowDownRight className="w-3 h-3" />
                            <span>-1.2%</span>
                        </div>
                    </CardContent>
                </Card>

                {/* 4. TAXA DE CONVERSÃO */}
                <Card className="border-l-4 border-l-purple-500 shadow-sm hover:shadow-md transition-all">
                    <CardHeader className="flex flex-row items-center justify-between pb-2 space-y-0">
                        <CardTitle className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Conversão</CardTitle>
                        <div className="p-1.5 bg-purple-50 dark:bg-purple-500/10 rounded-full">
                            <Activity className="w-3.5 h-3.5 text-purple-600" />
                        </div>
                    </CardHeader>
                    <CardContent>
                        <div className="text-2xl font-bold text-slate-900 dark:text-slate-50">28.4%</div>
                        <div className="flex items-center gap-1.5 mt-1 text-purple-600 text-xs font-semibold">
                            <ArrowUpRight className="w-3 h-3" />
                            <span>+4.1%</span>
                        </div>
                    </CardContent>
                </Card>

                {/* 5. OPORTUNIDADES (Pipeline) */}
                <Card className="border-l-4 border-l-slate-500 shadow-sm hover:shadow-md transition-all">
                    <CardHeader className="flex flex-row items-center justify-between pb-2 space-y-0">
                        <CardTitle className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Pipeline</CardTitle>
                        <div className="p-1.5 bg-slate-50 dark:bg-slate-500/10 rounded-full">
                            <Briefcase className="w-3.5 h-3.5 text-slate-600" />
                        </div>
                    </CardHeader>
                    <CardContent>
                        <div className="text-2xl font-bold text-slate-900 dark:text-slate-50">156</div>
                        <div className="flex items-center gap-1.5 mt-1 text-slate-600 text-xs font-semibold truncate">
                            {formatCurrency(450000)} em aberto
                        </div>
                    </CardContent>
                </Card>
            </div>

            {/* Main Charts Row */}
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
                {/* Sales Evolution Area Chart (2/3) */}
                <Card className="lg:col-span-2 shadow-sm overflow-hidden">
                    <CardHeader className="flex flex-row items-center justify-between">
                        <div>
                            <CardTitle>Crescimento de Receita</CardTitle>
                            <CardDescription>Evolução diária deste mês</CardDescription>
                        </div>
                    </CardHeader>
                    <CardContent className="h-[350px] w-full pt-4">
                        <ResponsiveContainer width="100%" height="100%">
                            <AreaChart data={dataSales}>
                                <defs>
                                    <linearGradient id="colorRevenue" x1="0" y1="0" x2="0" y2="1">
                                        <stop offset="5%" stopColor="#59A52C" stopOpacity={0.3} />
                                        <stop offset="95%" stopColor="#59A52C" stopOpacity={0} />
                                    </linearGradient>
                                </defs>
                                <CartesianGrid strokeDasharray="3 3" vertical={false} opacity={0.3} />
                                <XAxis
                                    dataKey="name"
                                    axisLine={false}
                                    tickLine={false}
                                    tick={{ fontSize: 12, fill: '#64748b' }}
                                    dy={10}
                                />
                                <YAxis
                                    axisLine={false}
                                    tickLine={false}
                                    tick={{ fontSize: 12, fill: '#64748b' }}
                                    tickFormatter={(val) => `R$${val / 1000}k`}
                                />
                                <Tooltip
                                    contentStyle={{ borderRadius: '12px', border: 'none', boxShadow: '0 10px 15px -3px rgb(0 0 0 / 0.1)' }}
                                    formatter={(val) => formatCurrency(val as number)}
                                />
                                <Area
                                    type="monotone"
                                    dataKey="revenue"
                                    stroke="#59A52C"
                                    strokeWidth={3}
                                    fillOpacity={1}
                                    fill="url(#colorRevenue)"
                                    animationDuration={1500}
                                />
                            </AreaChart>
                        </ResponsiveContainer>
                    </CardContent>
                </Card>

                {/* Funnel Chart (1/3) */}
                <Card className="shadow-sm">
                    <CardHeader>
                        <CardTitle>Funil de Vendas</CardTitle>
                        <CardDescription>Eficiência por etapa</CardDescription>
                    </CardHeader>
                    <CardContent className="h-[350px] relative flex flex-col justify-center p-0">
                        <CylindricalFunnel />
                    </CardContent>
                </Card>
            </div>

            {/* Bottom Section: Appointments & CRM Feed */}
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                {/* Today's Appointments */}
                <Card className="shadow-sm overflow-hidden flex flex-col">
                    <CardHeader className="bg-white dark:bg-card pb-4 pt-6 px-6 border-b border-slate-50 dark:border-border/50">
                        <div className="flex justify-between items-center">
                            <div>
                                <CardTitle className="bg-gradient-to-br from-emerald-950 to-black bg-clip-text text-transparent uppercase text-[12px] tracking-[0.2em] font-bold">Agenda de Hoje</CardTitle>
                                <CardDescription className="text-xs">Atendimentos confirmados e chegadas</CardDescription>
                            </div>
                            <Button variant="ghost" size="sm" className="text-[10px] uppercase tracking-wider font-semibold h-7 gap-1">
                                Ver Agenda <ChevronRight className="w-3 h-3" />
                            </Button>
                        </div>
                    </CardHeader>
                    <CardContent className="p-0 flex-1">
                        <div className="max-h-[350px] overflow-auto">
                            {todayAppointments.map((appt, idx) => (
                                <div key={appt.id} className="group transition-colors">
                                    <div className="p-5 flex items-center justify-between hover:bg-slate-50/50 dark:hover:bg-muted/30 transition-colors">
                                        <div className="flex items-center gap-4">
                                            <div className="flex flex-col items-center justify-center bg-slate-50 dark:bg-muted/50 rounded-lg p-2 min-w-[60px] border border-slate-100 dark:border-border/50">
                                                <span className="text-sm font-medium text-slate-700">{appt.time}</span>
                                            </div>
                                            <div className="flex flex-col">
                                                <span className="font-semibold text-sm group-hover:text-primary transition-colors">{appt.patient}</span>
                                                <span className="text-xs text-muted-foreground">{appt.service} • {appt.professional}</span>
                                            </div>
                                        </div>
                                        <Badge
                                            variant="outline"
                                            className={cn(
                                                "capitalize text-[10px] font-bold border-0 shadow-none px-3 py-1",
                                                appt.status === 'confirmed' ? 'bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400' :
                                                    appt.status === 'arrived' ? 'bg-blue-50 text-blue-700 dark:bg-blue-500/10 dark:text-blue-400' :
                                                        'bg-amber-50 text-amber-700 dark:bg-amber-500/10 dark:text-amber-400'
                                            )}
                                        >
                                            {
                                                appt.status === 'confirmed' ? 'Confirmado' :
                                                    appt.status === 'arrived' ? 'Chegou' :
                                                        'Pendente'
                                            }
                                        </Badge>
                                    </div>
                                    {idx !== todayAppointments.length - 1 && (
                                        <div className="mx-5 border-b border-slate-200/60" />
                                    )}
                                </div>
                            ))}
                        </div>
                    </CardContent>
                </Card>

                {/* CRM Recent Feed */}
                <Card className="shadow-sm flex flex-col">
                    <CardHeader className="bg-white dark:bg-card pb-4 pt-6 px-6 border-b border-slate-50 dark:border-border/50">
                        <div className="flex justify-between items-center">
                            <div>
                                <CardTitle className="bg-gradient-to-br from-emerald-950 to-black bg-clip-text text-transparent uppercase text-[12px] tracking-[0.2em] font-bold">Atividade do CRM</CardTitle>
                                <CardDescription className="text-xs">Movimentação recente</CardDescription>
                            </div>
                            <div className="flex items-center gap-2">
                                <span className="relative flex h-2 w-2">
                                    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                                    <span className="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
                                </span>
                                <span className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Live</span>
                            </div>
                        </div>
                    </CardHeader>
                    <CardContent className="p-0 flex-1">
                        <div className="max-h-[350px] overflow-auto">
                            {crmRecent.map((item, idx) => (
                                <div key={item.id} className="group transition-colors">
                                    <div className="p-5 flex gap-4 hover:bg-slate-50/50 dark:hover:bg-muted/30 transition-colors">
                                        <div className="shrink-0 bg-white dark:bg-card border border-slate-100 dark:border-border/50 shadow-sm rounded-full p-2 h-9 w-9 flex items-center justify-center">
                                            {item.type === 'sale' ? <Zap className="w-4 h-4 text-emerald-500 fill-emerald-500" /> :
                                                item.type === 'schedule' ? <Calendar className="w-4 h-4 text-blue-500" /> :
                                                    <Users className="w-4 h-4 text-slate-400" />}
                                        </div>
                                        <div className="flex flex-col flex-1">
                                            <div className="flex justify-between items-start">
                                                <div>
                                                    <p className="text-sm font-medium">
                                                        {item.action}{" "}
                                                        <span className={cn(
                                                            "font-semibold",
                                                            item.type === 'sale' ? "text-emerald-700 dark:text-emerald-400" : "text-foreground"
                                                        )}>
                                                            • {item.name}
                                                        </span>
                                                    </p>
                                                    <p className="text-[11px] text-muted-foreground/80 mt-0.5">{item.time}</p>
                                                </div>
                                                <div className="text-right">
                                                    {item.value > 0 && item.type === 'sale' && (
                                                        <span className="text-sm font-bold text-emerald-600">
                                                            +{formatCurrency(item.value)}
                                                        </span>
                                                    )}
                                                    {item.value > 0 && item.type !== 'sale' && (
                                                        <span className="text-[11px] font-semibold text-slate-400">
                                                            {formatCurrency(item.value)}
                                                        </span>
                                                    )}
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    {idx !== crmRecent.length - 1 && (
                                        <div className="mx-5 border-b border-slate-200/60" />
                                    )}
                                </div>
                            ))}
                        </div>
                    </CardContent>
                </Card>
            </div>
        </div>
    );
}
