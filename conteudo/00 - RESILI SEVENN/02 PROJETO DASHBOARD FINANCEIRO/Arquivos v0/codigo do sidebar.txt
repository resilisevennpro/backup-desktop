  // 6. Sidebar Content (DENTRO do componente)
  // ======================
  const SidebarContent = () => {
    return (
      <div className="flex flex-col h-full">
        <div className="flex items-center gap-3 mb-8">
          <div className="w-10 h-10 flex items-center justify-center flex-shrink-0">
            <Image 
              src="/logo-resili-fundo-transparente.png" 
              alt="Logo" 
              width={40} 
              height={40} 
              className="object-contain" 
            />
          </div>

          {!sidebarCollapsed && (
            <div>
              <h1 className="text-lg font-bold text-slate-900 ">Resili Sevenn</h1>
              <p className="text-sm text-slate-500">Dashboard Financeiro</p>
            </div>
          )}
        </div>

        <div className="mb-8">
          <Button
            variant="ghost"
            className={`w-full justify-start gap-2 hover:bg-slate-100 ${
              sidebarCollapsed ? "justify-center" : ""
            }`}
            onClick={() => setSidebarCollapsed((p) => !p)}
          >
            {sidebarCollapsed ? <PanelLeft className="w-5 h-5" /> : <PanelLeftClose className="w-5 h-5" />}
            {!sidebarCollapsed && <span>Recolher Menu</span>}
          </Button>
        </div>

        <div className="space-y-6">
          <div>
            {!sidebarCollapsed && (
              <h3 className="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-4">Filtros Rápidos</h3>
            )}

            <div className="space-y-4">
              <div>
                {!sidebarCollapsed && <label className="text-sm font-medium text-slate-700 mb-2 block">Período</label>}
                <Select value={selectedMonth} onValueChange={(v: any) => handlePeriodChange(v)}>
                  <SelectTrigger className="w-full bg-white border-slate-200">
                    <SelectValue placeholder="Selecione período" />
                  </SelectTrigger>
                  <SelectContent>
                    <SelectItem value="este-mes">Este Mês</SelectItem>
                    <SelectItem value="mes-passado">Mês Passado</SelectItem>
                    <SelectItem value="ultimos-7">Últimos 7 Dias</SelectItem>
                    <SelectItem value="ultimos-14">Últimos 14 Dias</SelectItem>
                    <SelectItem value="todo-historico">Todo Histórico</SelectItem>
                    <SelectItem value="personalizado">Personalizado</SelectItem>
                  </SelectContent>
                </Select>

                {showCustomDates && !sidebarCollapsed && (
                  <div className="mt-3 grid grid-cols-2 gap-2">
                    <div>
                      <label className="text-xs text-slate-500 mb-1 block">Início</label>
                      <Input
                        type="date"
                        value={startDate}
                        onChange={(e) => setStartDate(e.target.value)}
                        className="bg-white border-slate-200"
                      />
                    </div>
                    <div>
                      <label className="text-xs text-slate-500 mb-1 block">Fim</label>
                      <Input
                        type="date"
                        value={endDate}
                        onChange={(e) => setEndDate(e.target.value)}
                        className="bg-white border-slate-200"
                      />
                    </div>
                  </div>
                )}
              </div>

              <div>
                {!sidebarCollapsed && (
                  <label className="text-sm font-medium text-slate-700 mb-2 block">Profissional</label>
                )}
                <Select
                  value={selectedProfessionals.length === 0 ? "todos" : "selecione"} 
                  onValueChange={(value) => {
                    if (value === "todos") {
                      setSelectedProfessionals([])
                      return
                    }
                    if (value !== "selecione" && !selectedProfessionals.includes(value)) {
                      setSelectedProfessionals((prev) => [...prev, value])
                    }
                  }}
                >
                  <SelectTrigger className="w-full bg-white border-slate-200">
                    <div className="flex items-center gap-2 overflow-hidden">
                       {selectedProfessionals.length === 0 ? (
                          <span className="text-[#59A52C] font-medium flex items-center gap-1">
                             Todos Profissionais
                          </span>
                       ) : (
                          <span>Adicionar +</span>
                       )}
                    </div>
                  </SelectTrigger>
                  <SelectContent>
                    <SelectItem value="todos" className="text-[#59A52C] font-medium">
                        <span className="flex items-center gap-2">
                           {selectedProfessionals.length === 0 && <Check className="w-3 h-3"/>}
                           Todos os Profissionais
                        </span>
                    </SelectItem>
                    {dynamicProfessionals.map((prof, idx) => (
                         <SelectItem key={`${prof}-${idx}`} value={prof}>{prof}</SelectItem>
                    ))}
                    {dynamicProfessionals.length === 0 && (
                        <div className="px-2 py-2 text-xs text-slate-400">Carregando lista...</div>
                    )}
                  </SelectContent>
                </Select>

                {!sidebarCollapsed && (
                  <div className="flex flex-wrap gap-2 mt-3">
                    {selectedProfessionals.map((prof) => (
                      <div
                        key={prof}
                        className="inline-flex items-center gap-2 px-3 py-1.5 bg-[#59A52C] text-white rounded-full text-xs font-medium"
                      >
                        <span>{prof}</span>
                        <button
                          onClick={() => removeProfessional(prof)}
                          className="hover:bg-white/20 rounded-full p-0.5"
                        >
                          <X className="w-3 h-3" />
                        </button>
                      </div>
                    ))}
                  </div>
                )}
              </div>

              <div>
                {!sidebarCollapsed && (
                  <label className="text-sm font-medium text-slate-700 mb-2 block">Procedimento</label>
                )}
                <Select value={selectedProcedure} onValueChange={(v) => setSelectedProcedure(v)}>
                  <SelectTrigger className="w-full bg-white border-slate-200">
                    <SelectValue placeholder="Procedimento" />
                  </SelectTrigger>
                  <SelectContent>
                    <SelectItem value="all">Todos Procedimentos</SelectItem>
                    {dynamicProcedures.map((proc, idx) => (
                         <SelectItem key={`${proc}-${idx}`} value={proc}>{proc}</SelectItem>
                    ))}
                  </SelectContent>
                </Select>
              </div>
            </div>
          </div>
        </div>

        <div className="mt-auto pt-6 border-t border-slate-200">
          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <button
                className={`w-full flex items-center gap-3 p-2 rounded-lg hover:bg-slate-100 transition-colors ${
                  sidebarCollapsed ? "justify-center" : ""
                }`}
              >
                <div className="w-10 h-10 bg-[#6fca3a] rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-white font-bold text-sm">
                      {userName !== "Carregando..." ? userName.slice(0, 2).toUpperCase() : "..."}
                  </span>
                </div>

                {!sidebarCollapsed && (
                  <>
                    <div className="flex-1 text-left overflow-hidden">
                      <p className="text-sm font-semibold text-slate-900 truncate" title={userName}>{userName}</p>
                      <p className="text-xs text-slate-500">Minha Conta</p>
                    </div>
                    <ChevronsUpDown className="w-4 h-4 text-slate-400" />
                  </>
                )}
              </button>
            </DropdownMenuTrigger>

            <DropdownMenuContent align="start" side="top" className="w-56">
              <DropdownMenuItem
                onClick={() => {
                  setPasswordError(null)
                  setPasswordSuccess(null)
                  setCurrentPassword("")
                  setNewPassword("")
                  setShowPasswordDialog(true)
                }}
                className="flex items-center gap-2 cursor-pointer"
              >
                <KeyRound className="w-4 h-4" />
                <span>Alterar Senha</span>
              </DropdownMenuItem>
              <DropdownMenuItem
                onClick={() => setShowLogoutConfirm(true)}
                className="flex items-center gap-2 cursor-pointer text-red-600 focus:text-red-600"
              >
                <LogOut className="w-4 h-4" />
                <span>Encerrar Sessão</span>
              </DropdownMenuItem>
            </DropdownMenuContent>
          </DropdownMenu>

          {showPasswordDialog && !sidebarCollapsed && (
            <div className="mt-4 p-3 rounded-xl border border-slate-200 bg-white">
              <p className="text-sm font-semibold text-slate-900 mb-2">Alterar Senha</p>
              {passwordError && (
                 <p className="text-sm text-red-600 mb-2">{passwordError}</p>
              )}
              {passwordSuccess && (
                 <p className="text-sm text-emerald-600 mb-2">{passwordSuccess}</p>
              )}

              <div className="space-y-2">
                <Input
                  type="password"
                  placeholder="Senha Atual"
                  value={currentPassword}
                  onChange={(e) => setCurrentPassword(e.target.value)}
                  className="bg-white border-slate-200"
                />
                <Input
                  type="password"
                  placeholder="Nova Senha"
                  value={newPassword}
                  onChange={(e) => setNewPassword(e.target.value)}
                  className="bg-white border-slate-200"
                />
                <div className="flex items-center gap-2">
                  <Button onClick={handleChangePassword} disabled={passwordSaving} className="bg-[#59A52C] hover:bg-[#4a8c24] text-white w-full">
                    {passwordSaving ? "Salvando..." : "Salvar"}
                  </Button>
                  <Button variant="outline" onClick={() => setShowPasswordDialog(false)}>
                    Cancelar
                  </Button>
                </div>
              </div>
            </div>
          )}
        </div>
      </div>
    )
  }